const CACHE='perimeter-v3.13.1';
const CORE=['./','./index.html','./styles.css?v=3.13.1','./app.js?v=3.13.1','./manifest.webmanifest','./icons/icon.svg','./icons/icon-192.png','./icons/icon-512.png','./data/scenes.json','./data/documents.json','./data/expansion.json','./data/intimate-expansion.json','./data/keyframes.json'];
const KEY_FRAMES=['act1_bus','gates_closed','admin_hall','sergey_voice','empty_seat','archive_boxes','server_radio','underground_door','emergency_mode','central_archive','zorin_glass','final_choice','ending_truth','ending_lie','ending_sealed','ending_system'].map(name=>`./assets/images/${name}.webp`);
self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll([...CORE,...KEY_FRAMES])).then(()=>self.skipWaiting())));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
async function fallback(request,navigation){const cache=await caches.open(CACHE);return(await cache.match(request))||(await cache.match(request,{ignoreSearch:true}))||(navigation?await cache.match('./index.html'):undefined)}
async function networkFirst(request,navigation){const cache=await caches.open(CACHE);try{const response=await fetch(request,{cache:'no-store'});if(response.ok&&(response.type==='basic'||response.type==='default'))await cache.put(request,response.clone());return response}catch(error){const cached=await fallback(request,navigation);if(cached)return cached;throw error}}
self.addEventListener('fetch',event=>{const request=event.request,url=new URL(request.url);if(request.method!=='GET'||url.origin!==self.location.origin)return;const critical=request.mode==='navigate'||request.destination==='script'||request.destination==='style'||url.pathname.endsWith('.json');if(critical){event.respondWith(networkFirst(request,request.mode==='navigate'));return}event.respondWith(caches.match(request).then(hit=>hit||fetch(request).then(response=>{if(response.ok&&response.type==='basic'){const copy=response.clone();event.waitUntil(caches.open(CACHE).then(cache=>cache.put(request,copy)))}return response}).catch(()=>fallback(request,false))))});
